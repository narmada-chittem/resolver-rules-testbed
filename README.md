# resolver-rules-testbed

This is a seeded testbed, not a product codebase, used to drive the combined resolver+ingestion manual test matrix covering stages S1/S2/S3; it backs `.resolver` stage verification (IONE-1119), and its source tree currently contains only `src/greeter.ts`, which exports the `greet` and `farewell` functions.
